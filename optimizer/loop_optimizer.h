/**
 * Loop Optimizer:
 * ----------------
 * Detects loops with invariant computations, nested loops that could be flattened,
 * or loops that could benefit from unrolling to reduce runtime overhead.
 */
